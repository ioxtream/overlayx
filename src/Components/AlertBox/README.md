### Creating new IOXtream AlertBox Component

Run the command below and answer the prompted questions:

``` bash
$ npm run generate-alertbox
```

New files will be created at `src/Components/AlertBox`
